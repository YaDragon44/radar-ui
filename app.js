(()=>{"use strict";const today=Array.isArray(window.RADAR_TODAY)?window.RADAR_TODAY.slice(0,7):[];const portfolios=Array.isArray(window.RADAR_PORTFOLIO)?window.RADAR_PORTFOLIO.filter(i=>i&&i.context==="PORTFOLIO"&&i.portfolio):[];const decisions=[...today,...portfolios];const list=document.getElementById("today-list"),empty=document.getElementById("empty-state"),summary=document.querySelector(".summary"),market=document.querySelector(".market-context"),panelHeading=document.querySelector(".section-heading");const text=(v,f="—")=>v===null||v===undefined||v===""?f:String(v);const rub=v=>Number.isFinite(v)?new Intl.NumberFormat("ru-RU").format(v)+" ₽":"—";const pct=(v,b)=>Number.isFinite(v)&&Number.isFinite(b)&&b!==0?(v/b*100).toFixed(1).replace(".",",")+"%":"—";const itemCount=document.getElementById("item-count"),criticalCount=document.getElementById("critical-count");const tickerMap={"Артген":"ABIO","Астра":"ASTR","ВК":"VKCO","М.Видео":"MVID","Мечел":"MTLR","ММК":"MAGN","НМТП":"NMTP","Новатэк":"NVTK","Роснефть":"ROSN","Россети":"FEES","ТГК-1":"TGKA","ЦИАН":"CNRU","Фонд Тинькофф Индекс Мосбиржи":"TMOS"};const marketDecision=today.find(i=>i.context==="MARKET");if(marketDecision){document.getElementById("market-action").textContent=text(marketDecision.action,text(marketDecision.status,"NO_DECISION"));document.getElementById("market-why").textContent=text(marketDecision.why);document.getElementById("market-quality").textContent=text(marketDecision.quality,"UNKNOWN");document.getElementById("market-time").textContent=text(marketDecision.published_at)}const timestamps=decisions.map(i=>i.published_at).filter(Boolean).sort();document.getElementById("updated-at").textContent=timestamps.length?text(timestamps[timestamps.length-1]):"—";const safeSourceUrl=value=>{try{const u=new URL(String(value));return /^https?:$/.test(u.protocol)?u.href:null}catch{return null}};const createPortfolioWorkspace=d=>{const p=d.portfolio||{},positions=Array.isArray(p.positions)?p.positions:[],loss=Number.isFinite(p.drawdown_rub)?p.drawdown_rub:null,lossPct=pct(loss,p.contributed_rub),updated=d.published_at?new Date(d.published_at).toLocaleString("ru-RU",{day:"2-digit",month:"short",year:"numeric",hour:"2-digit",minute:"2-digit"}):"—",url=safeSourceUrl(d.source_ref),card=document.createElement("article");card.className="portfolio-workspace";const rows=positions.map(([name,qty],i)=>`<div class="pw-row"><span>${i+1}</span><span class="pw-paper"><b>${text(name)}</b><small>${text(tickerMap[name],"—")}</small></span><span>${text(tickerMap[name],"—")}</span><strong>${text(qty)}</strong><span>—</span><span>—</span><span>—</span><span>—</span><span>—</span><span>—</span></div>`).join("");card.innerHTML=`<section class="pw-hero"><div class="pw-crown">◆</div><div><div class="pw-live">PUBLIC PORTFOLIO · ${text(d.quality,"UNKNOWN")}</div><h2>${text(d.title,"Портфель")}</h2><p>Реальные данные источника. Без выдуманных цен и доходностей.</p><div class="pw-tags"><span>${text(p.week,"—")}-я неделя</span><span>${positions.length} позиций</span><span>Россия / RUB</span><span>Публичный</span></div></div><div class="pw-status"><b><i></i>${text(d.quality,"UNKNOWN")}</b><small>Обновлено<br>${updated}</small></div></section><section class="pw-kpis"><div class="pw-kpi pw-loss"><span>Стоимость портфеля</span><strong>${rub(p.value_rub)}</strong><em>−${rub(loss)} · ${lossPct}</em><small>к внесённым средствам</small></div><div class="pw-kpi"><span>Внесено всего</span><strong>${rub(p.contributed_rub)}</strong><small>собственные средства</small></div><div class="pw-kpi pw-green"><span>Дивиденды (всего)</span><strong>${rub(p.dividends_rub)}</strong><small>${pct(p.dividends_rub,p.contributed_rub)} от внесённого</small></div><div class="pw-kpi"><span>Свободные средства</span><strong>${rub(p.cash_rub)}</strong><small>${pct(p.cash_rub,p.value_rub)} от стоимости</small></div></section><section class="pw-analytics"><div class="pw-panel pw-chart"><header><h3>Динамика портфеля</h3><span>История</span></header><div class="pw-no-data"><b>Недостаточно исторических данных</b><p>График появится после накопления подтверждённых snapshots стоимости портфеля.</p></div></div><div class="pw-panel pw-structure"><header><h3>Структура портфеля</h3></header><div class="pw-no-data"><div class="pw-ring">${positions.length}<small>позиций</small></div><b>Секторные веса пока не рассчитаны</b><p>Нужны подтверждённые цены позиций.</p></div></div><div class="pw-panel pw-metrics"><header><h3>Ключевые метрики</h3></header><dl><div><dt>Доходность к внесённому</dt><dd class="red">−${lossPct}</dd></div><div><dt>Общий результат</dt><dd class="red">−${rub(loss)}</dd></div><div><dt>Дивиденды / внесено</dt><dd>${pct(p.dividends_rub,p.contributed_rub)}</dd></div><div><dt>Количество позиций</dt><dd>${positions.length}</dd></div><div><dt>Свободные средства</dt><dd>${rub(p.cash_rub)}</dd></div><div><dt>Валюта</dt><dd>RUB</dd></div></dl></div></section><section class="pw-positions"><header><div><span>ПОЗИЦИИ (${positions.length})</span><h3>Состав портфеля</h3></div>${url?`<a href="${url}" target="_blank" rel="noopener noreferrer">Источник #9180 ↗</a>`:""}</header><div class="pw-table"><div class="pw-row pw-head"><span>#</span><span>Бумага</span><span>Тикер</span><span>Кол-во</span><span>Средняя цена</span><span>Текущая цена</span><span>Стоимость</span><span>P/L</span><span>P/L %</span><span>Доля</span></div>${rows}</div><footer><span>Показано ${positions.length} из ${positions.length} позиций</span><b>Итого: ${rub(p.value_rub)} <em>−${rub(loss)} · ${lossPct}</em></b></footer></section>`;return card};const createDecisionCard=d=>{const c=document.createElement("article");c.className="decision-card";c.innerHTML=`<div class="decision-meta"><span>${text(d.context,"UNKNOWN")}</span><span>${text(d.priority,"UNKNOWN")}</span></div><h3>${text(d.action,text(d.status,"NO_DECISION"))}</h3><p class="decision-why">${text(d.why,"Недостаточно подтверждённых данных для решения.")}</p>`;return c};const renderView=(view="ALL")=>{const isPortfolio=view==="PORTFOLIO";let visible;if(isPortfolio)visible=portfolios;else if(view==="ALL")visible=today;else visible=today.filter(d=>d.context===view);document.body.classList.toggle("portfolio-mode",isPortfolio);list.classList.toggle("has-portfolio",isPortfolio&&visible.length>0);[summary,market,panelHeading].forEach(el=>{if(el)el.hidden=isPortfolio});list.replaceChildren(...(isPortfolio?visible.map(createPortfolioWorkspace):visible.map(createDecisionCard)));itemCount.textContent=String(visible.length);criticalCount.textContent=String(visible.filter(i=>i.priority==="CRITICAL").length);empty.hidden=visible.length>0};document.querySelectorAll(".view-tab").forEach(button=>button.addEventListener("click",()=>{document.querySelectorAll(".view-tab").forEach(tab=>{const active=tab===button;tab.classList.toggle("is-active",active);tab.setAttribute("aria-pressed",String(active))});renderView(button.dataset.view)}));window.RADAR_UI=Object.freeze({createDecisionCard,createPortfolioWorkspace,renderView,safeSourceUrl});renderView()})();